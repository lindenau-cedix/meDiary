<?xml version="1.0" encoding="utf-8"?>
<!--
  RemoteViews layout for the 1×1 tile. RemoteViews supports only a limited
  set of views (TextView, ImageView, FrameLayout, LinearLayout, and so on).
  The background color is set at runtime with
  `setInt(R.id.widget_root, "setBackgroundColor", …)`; the rounded-corner
  shape is defined in `widget_background.xml`.

  Two stacked TextViews: substance name (bold, 11sp) and amount (9sp,
  slightly transparent). `maxLines=1` + `ellipsize=end` handles very long
  substance names.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:padding="6dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center">

        <TextView
            android:id="@+id/widget_substance"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFFFF"
            android:textSize="11sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            android:gravity="center"
            tools:text="Quetiapin"
            xmlns:tools="http://schemas.android.com/tools" />

        <TextView
            android:id="@+id/widget_amount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFFCC"
            android:textSize="9sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:gravity="center"
            tools:text="50 mg"
            xmlns:tools="http://schemas.android.com/tools" />
    </LinearLayout>
</FrameLayout>
